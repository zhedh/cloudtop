export enum PerformanceType {
  TTFB = 'ttfb',
  READY = 'ready',
  LOAD = 'load',
  LCP = 'lcp',
}

export enum PerformanceApiType {
  TOTAL = 'total',
  TIME = 'time',
  SUCCESS = 'success',
  SLOW = 'slow',
}
